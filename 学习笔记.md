# 学习笔记.md


## 一、AI工具学习

### 1.1 Vibe Coding方法学习

Vibe Coding（氛围编程/自然语言驱动开发）是指通过自然语言与AI协作完成代码开发的编程范式。本课程设计将全程采用Vibe Coding方法完成系统开发。以下是对Vibe Coding核心方法的系统学习：

#### （1）提示词工程（Prompt Engineering）

提示词工程是Vibe Coding的基础能力，核心包括：
- **任务拆解**：将复杂需求分解为多个可独立实现的子任务，避免一次性让AI生成大量代码导致架构失控。
- **约束说明**：在提示词中明确技术栈、目录结构、命名规范、异常处理等工程约束。
- **验收标准**：为每个子任务定义明确的完成标准和测试用例，确保AI生成代码可验证。

Vibe Coding的效率上限不取决于提示词的精妙程度，而取决于前置落地的标准化工程规则。核心不是让AI自由生成代码，而是用人为预设的工程规则约束AI，让自然语言需求适配标准化代码仓库。

#### （2）上下文工程（Context Engineering）

上下文工程是将Vibe Coding的核心从Prompt编写升级为系统性的架构设计。关键实践包括：
- **项目规则文件**：在项目根目录建立规则文件（如`.claude.md`、项目规范文档），固化编码规范和架构约束。
- **让AI先读代码再改**：每次修改前让AI先理解现有代码结构，避免破坏已有逻辑。
- **会话压缩与上下文管理**：长对话中定期总结已有信息，防止AI遗忘早期架构约定。

#### （3）规格驱动开发（Spec-Driven Development, SDD）

规格驱动开发是以“规格”为核心的开发方法论：
1. **需求文档** → 先写清楚“我要什么”和“怎样算做好了”的规格
2. **AI制定计划** → AI根据规格拆解任务并制定实施计划
3. **分步实现** → 按照计划分步骤生成代码
4. **逐步验收** → 每完成一步对照规格进行验收

#### （4）AI代码的自动化测试与人工审查

- **自动化测试**：要求AI生成代码时同步生成单元测试和集成测试。
- **人工审查**：对AI生成的每一段代码进行人工审查，确保理解其逻辑。

#### （5）调试与人工接管策略

- **回滚**：使用Git随时回滚到稳定版本
- **缩小改动**：每次只让AI修改最小范围的代码
- **最小复现**：遇到Bug时先构造最小复现案例再让AI修复

### 1.2 Harness工具与编程大模型

#### Harness工具

Harness是指在AI模型之外的一整套工程外壳——让模型能读文件、调工具、管上下文、执行任务的系统。2026年AI编程工具的核心竞争已经从模型转向了Harness。

课程设计参考资料中提到的Harness工具包括：

| 工具 | 特点 |
|------|------|
| **Trae** | 字节跳动出品，市场份额达41.2%，完全免费 |
| **ZCode** | 智谱出品，深度适配GLM模型 |
| **Qwen Code** | 阿里出品，配合Qwen模型形成完整闭环 |
| **DeepSeek Harness** | DeepSeek开源，一切皆插件，四种运行模式 |
| **Claude Code** | Anthropic出品 |
| **Codex** | OpenAI出品 |

#### 编程大模型

| 模型 | 特点 |
|------|------|
| **DeepSeek-v4** | 国产大模型，Harness开源生态完善 |
| **GLM-5.3** | 智谱出品，与ZCode深度适配 |
| **Qwen 3.8** | 阿里出品，与Qwen Code配套 |
| **Kimi K3** | 月之暗面出品 |
| **Claude Code** | Anthropic出品，代码能力强 |

**本课程设计拟选用**：DeepSeek Harness + DeepSeek-v4 组合（开源、免费、功能完整），备选Trae + Qwen 3.8。


## 二、Git原理与功能

### 2.1 什么是Git

Git是一个分布式版本控制系统（Distributed Version Control System）。与集中式版本控制系统不同，Git中项目的本地克隆是一个完整的版本控制仓库，开发者可以在本地离线完成提交、查看历史等几乎所有操作。

### 2.2 Git的核心原理

**（1）直接记录快照，而非差异比较**

Git不像传统VCS那样记录文件的变化差异，而是将数据视为一系列文件系统的快照。每次提交（commit）时，Git会保存当前所有文件的状态快照，并存储一个指向该快照的引用。

**（2）数据完整性保证**

Git中所有的数据在存储前都会计算校验和（SHA-1哈希），然后以校验和来引用数据。这意味着任何文件的任何改动都会产生不同的哈希值，Git可以据此检测数据是否被篡改。

**（3）三种状态**

Git中的文件可能处于三种主要状态：
- **已提交（committed）** ：数据已安全保存在本地数据库中
- **已修改（modified）** ：文件已被修改但尚未提交
- **已暂存（staged）** ：修改后的文件已标记为下次提交的内容

### 2.3 Git的核心功能

| 功能 | 说明 |
|------|------|
| **版本回退** | 随时回退到任意历史版本 |
| **分支管理** | 创建、切换、合并、删除分支，支持并行开发 |
| **撤销与修改** | 撤销未提交的修改、修改提交信息等 |
| **远程协作** | 通过远程仓库实现多人协作 |
| **历史追溯** | 查看谁在什么时候改了什么 |

### 2.4 常用Git命令

```bash
# 配置用户信息
git config --global user.name "用户名"
git config --global user.email "邮箱"

# 初始化仓库
git init

# 查看状态
git status

# 添加文件到暂存区
git add <文件名>    # 添加单个文件
git add .           # 添加所有文件

# 提交
git commit -m "提交说明"

# 远程操作
git remote add origin <远程仓库URL>
git push -u origin main
git pull
```


## 三、选题调研

### 3.1 制造智能技术课程设计选题要求

根据课程设计任务书，选题须满足以下要求：

1. **场景**：须为制造/工业环节的智能化应用（设计、生产、质量控制、设备运维、物流仓储、生产管理等），具有真实业务感
2. **内容**：运用课程所学至少3章相关技术手段
3. **架构**：实现完整B/S或C/S架构的可运行demo，包含前端UI、后端服务、数据库、算法模块

### 3.2 拟选题目

**题目**：基于机器学习的工业产品质量智能检测系统

**应用场景**：制造业产品质量控制环节。在生产线上，通过采集产品多维度的质量特征数据（尺寸、重量、表面缺陷等），利用机器学习模型进行产品质量的自动判定与分类。

**技术方向覆盖（≥3章）** ：

| 技术方向 | 在系统中的实际作用 |
|----------|-------------------|
| **机器学习/深度学习** | 构建产品质量分类/预测模型，对产品进行合格/不合格判定 |
| **数据预处理与特征工程** | 对传感器采集的原始数据进行清洗、标准化、特征提取 |
| **数据库技术** | 存储产品检测记录、模型参数、历史数据 |
| **Web可视化** | 前端展示检测结果、统计报表、趋势分析 |

**系统架构草图**：

```
┌─────────────────────────────────────────────────────┐
│                    前端UI（Web界面）                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────────┐ │
│  │ 数据上传  │ │ 检测结果  │ │ 统计报表/趋势图      │ │
│  └──────────┘ └──────────┘ └──────────────────────┘ │
└─────────────────────┬───────────────────────────────┘
                      │ API
┌─────────────────────▼───────────────────────────────┐
│                   后端服务（Flask/FastAPI）            │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────────┐ │
│  │ 业务逻辑  │ │ 模型服务  │ │ 数据接口            │ │
│  └──────────┘ └──────────┘ └──────────────────────┘ │
└─────────┬──────────────┬──────────────┬─────────────┘
          │              │              │
┌─────────▼──────┐ ┌─────▼──────┐ ┌───▼─────────────┐
│   数据库        │ │  算法模块   │ │   文件存储       │
│  (MySQL/SQLite)│ │ (PyTorch/  │ │  (检测记录/     │
│  检测记录/      │ │  sklearn)  │ │   模型文件)      │
│  参数/用户      │ │  模型训练/  │ │                  │
│                 │ │  推理预测   │ │                  │
└────────────────┘ └────────────┘ └──────────────────┘
```

**数据来源**：
- 公开工业数据集（如UCI Machine Learning Repository中的工业制造数据集）
- 飞桨AI Studio（https://aistudio.baidu.com/）
- 魔搭社区（https://modelscope.cn）
- 模拟生成符合工业场景的合成数据
